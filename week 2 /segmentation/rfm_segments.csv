import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# Load RFM final output
file_path = r"D:\Zaalima\Week 2\week2_logic_core\rfm_analysis\rfm_final_output.csv"
df = pd.read_csv(file_path)

print("Data Loaded Successfully")
print(df.head())


# Remove unwanted index column if exists
if 'Unnamed: 0' in df.columns:
    df = df.drop(columns=['Unnamed: 0'])


# Check required columns
required_columns = ['Segment', 'Recency', 'Frequency', 'Monetary', 'CLV']

for col in required_columns:
    if col not in df.columns:
        print(f"Error: Missing column -> {col}")
        exit()


# Segment count
print("\nSegment Counts:")
print(df['Segment'].value_counts())


# Segment summary
segment_summary = df.groupby('Segment').agg({
    'Recency': 'mean',
    'Frequency': 'mean',
    'Monetary': 'mean',
    'CLV': 'mean'
}).round(2)

print("\nSegment Summary:")
print(segment_summary)


# Save segmented customer file
output_path_1 = r"D:\Zaalima\Week 2\week2_logic_core\segmentation\rfm_segments.csv"
df.to_csv(output_path_1, index=False)


# Save segment summary
output_path_2 = r"D:\Zaalima\Week 2\week2_logic_core\segmentation\segment_summary.csv"
segment_summary.to_csv(output_path_2)


# Visualization 1: Segment Distribution
plt.figure(figsize=(8,5))
sns.countplot(x='Segment', data=df)
plt.title("Customer Segment Distribution")
plt.xticks(rotation=20)
plt.tight_layout()
plt.show()


# Visualization 2: Monetary by Segment
plt.figure(figsize=(8,5))
sns.boxplot(x='Segment', y='Monetary', data=df)
plt.title("Monetary Distribution by Segment")
plt.xticks(rotation=20)
plt.tight_layout()
plt.show()


print("Segmentation Module Completed Successfully")
